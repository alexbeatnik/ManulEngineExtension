{
  "name": "manul-engine-extension-workspace",
  "version": "0.0.9.26",
  "private": true,
  "description": "ManulEngine VS Code extension workspace",
  "workspaces": [
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run compile -w manul-engine",
    "test": "npm run test --workspaces --if-present",
    "build:extension": "npm run compile -w manul-engine"
  }
}
