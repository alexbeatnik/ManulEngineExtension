{
  "name": "@manul/shared",
  "version": "0.0.9.26",
  "private": true,
  "description": "Shared types, constants, and parsers for ManulEngine tooling",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.0"
  }
}
